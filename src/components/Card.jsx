/* eslint-disable react/prop-types */


// eslint-disable-next-line no-unused-vars
const Card = ({coffee}) => {
    console.log(coffee);
    
    return (
        <div>
          Card  
        </div>
    );
};

export default Card;